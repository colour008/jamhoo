---
title: 算法学习笔记🐌
date: 2023-01-26 20:02:56
updated: 2024-11-30 18:00:00
tag: [算法,学习笔记]
categories: [算法,学习笔记]
cover: 
description: 算法学习笔记🐌
sticky: 5
swiper_index: 5
---
---

<span style="color:#FF3333;">算法</span>一词代指解决问题的方法，换句话说，算法提供了一整套解决问题的方案或者思路，即先做什么，再做什么，最后做什么。学习算法，可以短时间内快速提升我们“遇到问题，解决问题”的能力。

> 笔记大部分内容和图例转载自[C语言中文网](http://c.biancheng.net/)，感谢站长的付出。

# 一、基础算法

## （一）二分查找

+ `定义`：二分查找也称折半查找（Binary Search），它是一种效率较高的查找方法。但是，折半查找要求线性表必须采用[顺序存储结构](https://baike.baidu.com/item/顺序存储结构/1347176?fromModule=lemma_inlink)，而且表中元素按索引有序排列。
+ `局限性`：1. Sorted（单调递增或者递减）；2. Bounded（存在上下边界）；3. Accessible by index（能够通过索引访问）。
+ `算法复杂度`：二分查找的基本思想是将n个元素分成大致相等的两部分，取a[n/2]与x(目标值)做比较，如果x=a[n/2],则找到x,算法中止；如果x<a[n/2],则只要在数组a的左半部分继续搜索x,如果x>a[n/2],则只要在数组a的右半部搜索x。
+ `伪代码`：如下用给大家展示了二分查找算法的具体实现过程：

```json
输入 arr[]                                    // 输入有序序列
binary_search( arr , begin , end , target):  // [begin,end]指定搜索区域，target为要搜索的目标元素
    if begin > end:                          // [begin,end]不存在时，返回一个错误值（比如-1）
        return -1
    mid <- ⌊ begin+(end-begin)/2 ⌋           // 找到[begin,end]区域内中间元素所在位置的下标
    if target == arr[mid]:                   // 递归的出口，即target和中间元素的值相等
        return mid
    if target ＜ arr[mid]:                   // 比较target和中间元素的值，进一步缩小搜索区域
        return binary_search(arr , begin , mid-1 , target)
    else:
        return binary_search(arr , mid+1 , end , target)
```

+ `示例代码`:C语言用二分查找算法找出目标值索引的示例代码如下：

```c++
  #include <stdio.h>
  #include <stdlib.h>
  
  int binary_search(int *arr, int left, int right, int target)
  {
  	int mid = 0;
  		if (left > right)
  	{
  		return -1;  // 如果[left,right] 不存在，返回 -1
  	}
  	/* 找到中间元素所在的位置,使用(left+right)/2会有整数溢出的问题问题会出现在当left+right的结果大于表达式结果类型所能表示的最大值时，这样，产生溢出后再除2是不会产生正确结果的，而left + (right - left) / 2不存在这个问题 */
  	mid = left + (right - left) / 2;
  	if (target == arr[mid])  // 递归的出口
  	{
  		return mid;
  	}
  		if (target < arr[mid])  // 比较 target 和 arr[mid] 的值，缩小 target 可能存在的区域
  	{
  	
  		return binary_search(arr, left, mid - 1, target);  // 新的搜索区域为 [left,mid-1]
  	}
  	else
  	{
  		return binary_search(arr, mid + 1, right, target);  // 新的搜索区域为 [mid+1,right]
  	}
  }
  int main()
  {
  	int binary_search(int *arr, int left, int right, int target);
  	int Array[10] = {1, 4, 7, 8, 9, 12, 13, 16, 18, 19};
  	int len = sizeof(Array) / sizeof(int);
  	int left = 0, right = len - 1;
  	int target;
  	printf("Array[10] = {1, 4, 7, 8, 9, 12, 13, 16, 18, 19}\n");
  	printf("在上面数组中挑一个数输入按回车键，可查找其索引值：");
  	scanf("%d", &target);
  	printf("索引值为%d\n", binary_search(Array, left, right, target));
  	system("pause");
  	return 0;
  }
```

## （二）分治算法

+ `定义`：“分治”即“分而治之”的意思。分治算法解决问题的思路是：先将整个问题拆分成多个相互独立且数据量更少的小问题，通过逐一解决这些简单的小问题，最终找到解决整个问题的方案。

  > 所谓问题间相互独立，简单理解就是每个问题都可以单独处理，不存在“谁先处理，谁后处理”的次序问题。
  >
+ `局限性`:使用分治算法解决的问题都具备这样的特征，当需要处理的数据量很少时，问题很容易就能解决，随着数据量增多，问题的解决难度也随之增大。

  分治算法的弊端也很明显，该算法经常和递归算法搭配使用，整个解决问题的过程会耗费较多的时间和内存空间，严重时还可能导致程序运行崩溃。
+ `示例图`：分治算法找数组的最大值和最小值：不断地等分数组中的元素，直至各个分组中元素的个数 ≤2。由于每个分组内的元素最多有 2 个，很容易就可以找出其中的最值（最大值或最小值），然后这些最值再进行两两比较，最终找到的最值就是整个数组中的最值。下图展示了用分治算法查找 {3, 7, 2, 1} 中最大值的实现过程：

  ![](https://bu.dusays.com/2024/12/08/67552150043ae.gif)
+ `伪代码`：如下是分治算法求数组中最大值的:

```json
输入 arr[1...n]            // 输入n个数字
arr_max(x , y) :          // 设计一个递归函数，[x,y] 用来限定查找最大数的范围
    if y-x ≤ 1 :          // 如果y-x的值小于等于1，则比较 arr[x]和arr[y]的值，大的就是最大值 
        return max(arr[x] , arr[y])
    else :
    // 将[x,y]区域划分为[x,⌊(x+y)/2⌋ ]和[⌊(x+y)/2+1⌋,y] 两个区域，求出两个区域内各自的最大值
        max1 = arr_max(x , ⌊(x+y)/2⌋ )  
        max2 = arr_max( ⌊(x+y)/2+1⌋ , y)
    return max(max1 , max2)   // 比较两个区域的最大值，最终找出[x,y] 中的最大值
```

* `示例代码`：C语言用分治算法实现“求数组中最大值”的代码如下：

```c++
#include <stdio.h>
#include <stdlib.h>

int get_max(int *arr, int left, int right)
{
    int max_left = 0, max_right = 0, middle = 0;
    if (arr == NULL)
    {
        return -1;  // 如果数组不存在
    }
    if (right - left == 0)
    {
        return arr[left];  // 如果查找范围中仅有一个数字
    }
    if (right - left <= 1)  // 如果查找范围中有 2 个数字，直接比较即可
    {
        if (arr[left] >= arr[right])
        {
            return arr[left];
        }
        return arr[right];
    }
    middle = (right - left) / 2 + left;  // 等量划分成 2 个区域
    max_left = get_max(arr, left, middle);  // 得到左侧区域中的最大值
    max_right = get_max(arr, middle + 1, right);  // 得到右侧区域中的最大值
    if (max_left >= max_right)    // 比较左、右两侧的最大值，找到 [left,right] 整个区域的最大值
    {
        return max_left;
    }
    else
    {
        return max_right;
    }
}

int main()
{
    int get_max(int *arr, int left, int right);
    int Arr[6] = {3, 7, 8, 5, 4, 9};
    int result = get_max(Arr, 0, 5);
    printf("%d\n", result);
    system("pause");
    return 0;
}
```

## （三）排序算法

### ◆ 桶排序

* `定义`：桶排序（又称箱排序）是一种基于分治思想、效率很高的排序算法。
* `思路`：假设一种场景，对 {5, 2, 1, 4, 3} 进行升序排序，桶排序算法的实现思路是：

    1.准备5个桶，从1~5对它们进行编号；

    2.将待排序序列的各个元素放置到相同编号的桶中；

    3.从1号桶开始，依次获取桶中放置的元素，得到的就是一个升序序列。

* `时间复杂度`：理想情况下对应的时间复杂度为O(n)。
* `示例代码`：只有一种元素的情况下，C语言桶排序示例代码如下：

```c++
#include <stdio.h>
#include <stdlib.h>
#define NUM 101 // 0~100内的整数，则需要101个桶
int main()
{
    int i, j, t;
    int mark[NUM];
    for (i = 0; i <= NUM - 1; i++) // 0~100号标记桶，初始化为0
    {
        mark[i] = 0;
    }

    int n;
    printf("输入需要排序的个数："); // 表示接下来有n个数排序
    scanf("%d", &n);

    printf("输入%d个0~100的整数：\n", n);
    for (i = 0; i < n; i++)
    {
        scanf("%d", &t); // 此处是关键，对输入的数，找到对应的桶进行标记和计数
        mark[t]++;
    }

    printf("从小到大排序结果为：\n");
    for (i = 0; i <= NUM - 1; i++) // 遍历全部桶，每个桶计数为多少，就输出多少次
    {
        for (j = 1; j <= mark[i]; j++)
        {
            printf("%-3d", i);
        }
    }
    printf("\n");

    printf("从大到小排序结果为：\n");
    for (i = NUM - 1; i >= 0; i--)
    {
        for (j = 1; j <= mark[i]; j++)
        {
            printf("%-3d", i);
        }
    }
    printf("\n");
    system("pause");
    return 0;
}
```

### ◆ 冒泡排序

* `定义`：冒泡排序(Bubble_sort)是所有排序算法中最简单、最易实现的算法，有时也称为==起泡排序==算法。使用冒泡排序算法对n个数据进行排序。
* `思路`：从待排序序列中找出一个最大值或最小值，这样的操作执行n-1次，最终就可以得到一个有序序列。
* `时间复杂度`:（n-1）^2^，记为O(n^2^)。
* `伪代码`：如下是冒泡排序算法实现升序排序：

```json
Bubble_sort(list):                //list表示待排序序列
    for i <- 0 to length(list)-1: //对元素个数为n的list序列，需遍历n-1次，这里用[0,length(list)-1)表示
        for j <- 1 to length(list) - i:    // 从第1个元素开始遍历，遍历区间为[1,length(list)-i)
            if list[j] > list[j+1]:        // 若进行降序排序，则改成<小于号
                 swap(list[j] , list[j+1]) // 交换2个相邻元素的位置
    return list                            // 返回排好序的序列
```

* `示例代码`：C语言冒泡排序降序示例代码如下：

```c++
#include <stdio.h>
#include <stdlib.h>

void Bubble_sort(int *array, int n) //实现冒泡降序排序算法的函数，array为待排序数组，n为数组元素个数
{
    int i, j, temp;
    for (i = 0; i < n - 1; i++) //n个元素，遍历n-1次，相邻两数两两向后遍历，n个数只需遍历n-1次就行
    {
        for (j = 0; j < n - i - 1; j++)  // 从第1个元素开始遍历，遍历至n-1-i
        {
            if (array[j] < array[j + 1]) //比较相邻两数的大小，进行元素位置交换
            {
                temp = array[j];
                array[j] = array[j + 1];
                array[j + 1] = temp;
            }
        }
    }
}

int main()
{
    void Bubble_sort(int *array, int n);
    int n;
    printf("输入排序的元素个数：");
    scanf("%d", &n);
    int *array = (int *)malloc(n * sizeof(int));
    printf("输入%d个数进行排序：", n);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &array[i]);
    }
    Bubble_sort(array, n);
    for (int i = 0; i < n; i++)
    {
        printf("%-3d", array[i]);
    }
    printf("\n");
    free(array);
    system("pause");
    return 0;
}
```

## （四）递归算法

* `定义`：将函数（方法）调用自身的过程称为递归，调用自身的函数称为递归函数，用递归方式解决问题的算法称为递归算法。涉及==栈==的知识，即“先进后出”。下图展示了函数递归调用的底层实现过程：

![](https://bu.dusays.com/2024/12/08/67552155f1dee.gif)

* `局限性`：设计递归函数时，我们必须为它设置一个结束递归的“出口”，否则函数会一直调用自身（死循环），直至运行崩溃。
* `伪代码`：分两种情况==直接调用==和==间接调用==：

> 直接调用自身

```json
int funciton(/*...*/) {
    //......
    //调用自身
    function(/*...*/);
    //......
}
```

> 间接调用自身

```json
int funciton1(/*...*/) {
    //......
    //调用另一个函数
    function2(/*...*/);
    //......
}
int function2(/*...*/) {
    //......
    //调用function1()函数
    funciton1(/*...*/);
    //......
}
```

`示例代码`：C语言递归算法求 `n!`示例代码如下：

```c++
#include <stdio.h>
#include <stdlib.h>

int factorial(int n)
{
    if (n == 1 || n == 0) // 递归的出口
    {
        return 1;
    }
    return n * factorial(n - 1); // 函数调用自身
}

int main()
{
    int n;
    printf("输入一个数，求阶乘值：");
    scanf("%d", &n);
    printf("%d! = %d\n", n, factorial(n));
    system("pause");
    return 0;
}
```

## （五）贪心算法

* `定义`：每一步都力求最大限度地解决问题，每一步都选择的是当前最优的解决方案，这种解决问题的算法就是贪心算法。
* `局限性`：虽然贪心算法每一步都是最优的解决方案，但整个算法并不一定是最优的。总的来讲，贪心算法注重的是每一步选择最优的解决方案（又称“局部最优解”），并不关心整个解决方案是否最优。
* `示例代码`：C语言谈心算法求==小孩分糖==问题示例代码如下：

> 题目引用自LeetCode[第455题](https://leetcode.cn/problems/assign-cookies/)

```c++
#include <stdio.h>
#include <stdlib.h>

void Bubble_sort(int *array, int lenth) // 排序
{
    int i, j, temp;
    for (i = 0; i < lenth - 1; i++)
    {
        for (j = 0; j < lenth - i - 1; j++)
        {
            if (array[j] > array[j + 1])
            {
                temp = array[j];
                array[j] = array[j + 1];
                array[j + 1] = temp;
            }
        }
    }
}

int find_target(int *child, int child_len, int *candy, int candy_len) // 计算满足小孩需求的最大数
{
    Bubble_sort(child, child_len);  // 小孩需求从小到大排序
    Bubble_sort(candy, candy_len);  // 糖果大小从小到大排序
    int i = 0;                      // 小孩需求索引
    int j = 0;                      // 糖果索引
    while (i < child_len && j < candy_len) // 糖果和小孩都没有完成尝试时，循环进行
    {
        if (candy[j] >= child[i]) // 小孩需求被满足时剔除，让下一个小孩继续尝试
        {
            i++;
        }
        j++;  // 糖果无论小孩需求满足与否，糖果都要逐个剔除
    }
    return i;
}

int main()
{
    void Bubble_sort(int *array, int lenth);
    int find_target(int *child, int child_len, int *candy, int candy_len);
    int child[6] = {6, 11, 3, 10, 16, 10};     // 小孩需求大小
    int candy[5] = {7, 2, 21, 4, 9};           // 糖果大小
    printf("最大可满足%d个小孩的需求\n", find_target(child, 6, candy, 5));
    system("pause");
    return 0;
}
```
